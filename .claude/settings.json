{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "git add -A && git diff --cached --quiet || (git commit -m \"auto-save: $(date '+%Y-%m-%d %H:%M')\" && git push origin main)",
            "shell": "bash",
            "statusMessage": "Saving to GitHub...",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
